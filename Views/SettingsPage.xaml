<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="ColorTubes.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:resources="clr-namespace:ColorTubes.Resources.Localization">

    <VerticalStackLayout Padding="16" Spacing="12">
        <Label Text="{x:Static resources:AppResources.Settings_Language}"
               FontSize="18" />

        <!-- Список языков -->
        <Picker x:Name="LangPicker" Title="{x:Static resources:AppResources.Settings_Language}">
            <Picker.Items>
                <x:String>ru — Русский</x:String>
                <x:String>et — Eesti</x:String>
                <x:String>en — English</x:String>
            </Picker.Items>
        </Picker>

        <Button Text="{x:Static resources:AppResources.Apply}"
                Clicked="OnApplyLangClicked"
                BackgroundColor="#2D3AFE"
                TextColor="White"
                CornerRadius="10" />

        <!-- по желанию -->
        <Label x:Name="HintLabel" Text="" FontSize="12" Opacity="0.8"/>
    </VerticalStackLayout>
</ContentPage>
