<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ColorTubes.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:ColorTubes.Views">

    <TabBar>
        <ShellContent Title="Game"   ContentTemplate="{DataTemplate views:GamePage}" />
        <ShellContent Title="Levels" ContentTemplate="{DataTemplate views:LevelsPage}" />
        <ShellContent Title="Settings" ContentTemplate="{DataTemplate views:SettingsPage}" />
    </TabBar>
</Shell>